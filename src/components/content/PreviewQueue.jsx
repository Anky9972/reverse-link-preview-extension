import React from 'react';

export const PreviewQueue = ({ queue }) => {
  // This component doesn't render anything visible
  // It's used to manage the queue of pending previews
  
  return null;
};